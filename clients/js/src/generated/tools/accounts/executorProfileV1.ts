/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type ExecutorProfileV1 = Account<ExecutorProfileV1AccountData>;

export type ExecutorProfileV1AccountData = {
  key: Key;
  padding: Array<number>;
  authority: PublicKey;
};

export type ExecutorProfileV1AccountDataArgs = {
  key: KeyArgs;
  authority: PublicKey;
};

export function getExecutorProfileV1AccountDataSerializer(): Serializer<
  ExecutorProfileV1AccountDataArgs,
  ExecutorProfileV1AccountData
> {
  return mapSerializer<
    ExecutorProfileV1AccountDataArgs,
    any,
    ExecutorProfileV1AccountData
  >(
    struct<ExecutorProfileV1AccountData>(
      [
        ['key', getKeySerializer()],
        ['padding', array(u8(), { size: 7 })],
        ['authority', publicKeySerializer()],
      ],
      { description: 'ExecutorProfileV1AccountData' }
    ),
    (value) => ({ ...value, padding: [0, 0, 0, 0, 0, 0, 0] })
  ) as Serializer<
    ExecutorProfileV1AccountDataArgs,
    ExecutorProfileV1AccountData
  >;
}

export function deserializeExecutorProfileV1(
  rawAccount: RpcAccount
): ExecutorProfileV1 {
  return deserializeAccount(
    rawAccount,
    getExecutorProfileV1AccountDataSerializer()
  );
}

export async function fetchExecutorProfileV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<ExecutorProfileV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'ExecutorProfileV1');
  return deserializeExecutorProfileV1(maybeAccount);
}

export async function safeFetchExecutorProfileV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<ExecutorProfileV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeExecutorProfileV1(maybeAccount)
    : null;
}

export async function fetchAllExecutorProfileV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<ExecutorProfileV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'ExecutorProfileV1');
    return deserializeExecutorProfileV1(maybeAccount);
  });
}

export async function safeFetchAllExecutorProfileV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<ExecutorProfileV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeExecutorProfileV1(maybeAccount as RpcAccount)
    );
}

export function getExecutorProfileV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'mplAgentTools',
    'TLREGni9ZEyGC3vnPZtqUh95xQ8oPqJSvNjvB7FGK8S'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      padding: Array<number>;
      authority: PublicKey;
    }>({
      key: [0, getKeySerializer()],
      padding: [1, array(u8(), { size: 7 })],
      authority: [8, publicKeySerializer()],
    })
    .deserializeUsing<ExecutorProfileV1>((account) =>
      deserializeExecutorProfileV1(account)
    );
}

export function getExecutorProfileV1Size(): number {
  return 40;
}
