/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type ExecutiveProfileV1 = Account<ExecutiveProfileV1AccountData>;

export type ExecutiveProfileV1AccountData = {
  key: Key;
  padding: Array<number>;
  authority: PublicKey;
};

export type ExecutiveProfileV1AccountDataArgs = {
  key: KeyArgs;
  authority: PublicKey;
};

export function getExecutiveProfileV1AccountDataSerializer(): Serializer<
  ExecutiveProfileV1AccountDataArgs,
  ExecutiveProfileV1AccountData
> {
  return mapSerializer<
    ExecutiveProfileV1AccountDataArgs,
    any,
    ExecutiveProfileV1AccountData
  >(
    struct<ExecutiveProfileV1AccountData>(
      [
        ['key', getKeySerializer()],
        ['padding', array(u8(), { size: 7 })],
        ['authority', publicKeySerializer()],
      ],
      { description: 'ExecutiveProfileV1AccountData' }
    ),
    (value) => ({ ...value, padding: [0, 0, 0, 0, 0, 0, 0] })
  ) as Serializer<
    ExecutiveProfileV1AccountDataArgs,
    ExecutiveProfileV1AccountData
  >;
}

export function deserializeExecutiveProfileV1(
  rawAccount: RpcAccount
): ExecutiveProfileV1 {
  return deserializeAccount(
    rawAccount,
    getExecutiveProfileV1AccountDataSerializer()
  );
}

export async function fetchExecutiveProfileV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<ExecutiveProfileV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'ExecutiveProfileV1');
  return deserializeExecutiveProfileV1(maybeAccount);
}

export async function safeFetchExecutiveProfileV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<ExecutiveProfileV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeExecutiveProfileV1(maybeAccount)
    : null;
}

export async function fetchAllExecutiveProfileV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<ExecutiveProfileV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'ExecutiveProfileV1');
    return deserializeExecutiveProfileV1(maybeAccount);
  });
}

export async function safeFetchAllExecutiveProfileV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<ExecutiveProfileV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeExecutiveProfileV1(maybeAccount as RpcAccount)
    );
}

export function getExecutiveProfileV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'mplAgentTools',
    'TLREGni9ZEyGC3vnPZtqUh95xQ8oPqJSvNjvB7FGK8S'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      padding: Array<number>;
      authority: PublicKey;
    }>({
      key: [0, getKeySerializer()],
      padding: [1, array(u8(), { size: 7 })],
      authority: [8, publicKeySerializer()],
    })
    .deserializeUsing<ExecutiveProfileV1>((account) =>
      deserializeExecutiveProfileV1(account)
    );
}

export function getExecutiveProfileV1Size(): number {
  return 40;
}

export function findExecutiveProfileV1Pda(
  context: Pick<Context, 'eddsa' | 'programs'>,
  seeds: {
    /** The address of the authority */
    authority: PublicKey;
  }
): Pda {
  const programId = context.programs.getPublicKey(
    'mplAgentTools',
    'TLREGni9ZEyGC3vnPZtqUh95xQ8oPqJSvNjvB7FGK8S'
  );
  return context.eddsa.findPda(programId, [
    string({ size: 'variable' }).serialize('executive_profile'),
    publicKeySerializer().serialize(seeds.authority),
  ]);
}

export async function fetchExecutiveProfileV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  seeds: Parameters<typeof findExecutiveProfileV1Pda>[1],
  options?: RpcGetAccountOptions
): Promise<ExecutiveProfileV1> {
  return fetchExecutiveProfileV1(
    context,
    findExecutiveProfileV1Pda(context, seeds),
    options
  );
}

export async function safeFetchExecutiveProfileV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  seeds: Parameters<typeof findExecutiveProfileV1Pda>[1],
  options?: RpcGetAccountOptions
): Promise<ExecutiveProfileV1 | null> {
  return safeFetchExecutiveProfileV1(
    context,
    findExecutiveProfileV1Pda(context, seeds),
    options
  );
}
