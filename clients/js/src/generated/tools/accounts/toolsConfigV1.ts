/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type ToolsConfigV1 = Account<ToolsConfigV1AccountData>;

export type ToolsConfigV1AccountData = {
  key: Key;
  bump: number;
  padding: Array<number>;
  admin: PublicKey;
  collection: PublicKey;
  nextTreeIndex: bigint;
};

export type ToolsConfigV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  admin: PublicKey;
  collection: PublicKey;
  nextTreeIndex: number | bigint;
};

export function getToolsConfigV1AccountDataSerializer(): Serializer<
  ToolsConfigV1AccountDataArgs,
  ToolsConfigV1AccountData
> {
  return mapSerializer<
    ToolsConfigV1AccountDataArgs,
    any,
    ToolsConfigV1AccountData
  >(
    struct<ToolsConfigV1AccountData>(
      [
        ['key', getKeySerializer()],
        ['bump', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['admin', publicKeySerializer()],
        ['collection', publicKeySerializer()],
        ['nextTreeIndex', u64()],
      ],
      { description: 'ToolsConfigV1AccountData' }
    ),
    (value) => ({ ...value, padding: [0, 0, 0, 0, 0, 0] })
  ) as Serializer<ToolsConfigV1AccountDataArgs, ToolsConfigV1AccountData>;
}

export function deserializeToolsConfigV1(
  rawAccount: RpcAccount
): ToolsConfigV1 {
  return deserializeAccount(
    rawAccount,
    getToolsConfigV1AccountDataSerializer()
  );
}

export async function fetchToolsConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<ToolsConfigV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'ToolsConfigV1');
  return deserializeToolsConfigV1(maybeAccount);
}

export async function safeFetchToolsConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<ToolsConfigV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeToolsConfigV1(maybeAccount) : null;
}

export async function fetchAllToolsConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<ToolsConfigV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'ToolsConfigV1');
    return deserializeToolsConfigV1(maybeAccount);
  });
}

export async function safeFetchAllToolsConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<ToolsConfigV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeToolsConfigV1(maybeAccount as RpcAccount)
    );
}

export function getToolsConfigV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'mplAgentTools',
    'TLREGni9ZEyGC3vnPZtqUh95xQ8oPqJSvNjvB7FGK8S'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      padding: Array<number>;
      admin: PublicKey;
      collection: PublicKey;
      nextTreeIndex: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      padding: [2, array(u8(), { size: 6 })],
      admin: [8, publicKeySerializer()],
      collection: [40, publicKeySerializer()],
      nextTreeIndex: [72, u64()],
    })
    .deserializeUsing<ToolsConfigV1>((account) =>
      deserializeToolsConfigV1(account)
    );
}

export function getToolsConfigV1Size(): number {
  return 80;
}

export function findToolsConfigV1Pda(
  context: Pick<Context, 'eddsa' | 'programs'>
): Pda {
  const programId = context.programs.getPublicKey(
    'mplAgentTools',
    'TLREGni9ZEyGC3vnPZtqUh95xQ8oPqJSvNjvB7FGK8S'
  );
  return context.eddsa.findPda(programId, [
    string({ size: 'variable' }).serialize('program_config'),
  ]);
}

export async function fetchToolsConfigV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  options?: RpcGetAccountOptions
): Promise<ToolsConfigV1> {
  return fetchToolsConfigV1(context, findToolsConfigV1Pda(context), options);
}

export async function safeFetchToolsConfigV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  options?: RpcGetAccountOptions
): Promise<ToolsConfigV1 | null> {
  return safeFetchToolsConfigV1(
    context,
    findToolsConfigV1Pda(context),
    options
  );
}
