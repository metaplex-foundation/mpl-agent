/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RevokeExecutionV1InstructionAccounts = {
  /** The execution delegate record to close */
  executionDelegateRecord: PublicKey | Pda;
  /** The agent asset */
  agentAsset: PublicKey | Pda;
  /** The destination for the refunded rent */
  destination: PublicKey | Pda;
  /** The payer */
  payer?: Signer;
  /** Authority — must be asset owner or executive authority */
  authority?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type RevokeExecutionV1InstructionData = {
  discriminator: number;
  padding: Array<number>;
};

export type RevokeExecutionV1InstructionDataArgs = {};

export function getRevokeExecutionV1InstructionDataSerializer(): Serializer<
  RevokeExecutionV1InstructionDataArgs,
  RevokeExecutionV1InstructionData
> {
  return mapSerializer<
    RevokeExecutionV1InstructionDataArgs,
    any,
    RevokeExecutionV1InstructionData
  >(
    struct<RevokeExecutionV1InstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 7 })],
      ],
      { description: 'RevokeExecutionV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 2, padding: [0, 0, 0, 0, 0, 0, 0] })
  ) as Serializer<
    RevokeExecutionV1InstructionDataArgs,
    RevokeExecutionV1InstructionData
  >;
}

// Instruction discriminator.
export const revokeExecutionV1InstructionDiscriminator = 2;

// Instruction.
export function revokeExecutionV1(
  context: Pick<Context, 'payer' | 'programs'>,
  input: RevokeExecutionV1InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplAgentTools',
    'TLREGni9ZEyGC3vnPZtqUh95xQ8oPqJSvNjvB7FGK8S'
  );

  // Accounts.
  const resolvedAccounts = {
    executionDelegateRecord: {
      index: 0,
      isWritable: true as boolean,
      value: input.executionDelegateRecord ?? null,
    },
    agentAsset: {
      index: 1,
      isWritable: false as boolean,
      value: input.agentAsset ?? null,
    },
    destination: {
      index: 2,
      isWritable: true as boolean,
      value: input.destination ?? null,
    },
    payer: {
      index: 3,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 4,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRevokeExecutionV1InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
