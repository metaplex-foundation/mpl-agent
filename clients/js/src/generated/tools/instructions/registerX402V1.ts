/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findX402EndpointV1Pda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RegisterX402V1InstructionAccounts = {
  /** The x402 endpoint account */
  x402Endpoint?: PublicKey | Pda;
  /** The agent MPL Core asset */
  agentAsset: PublicKey | Pda;
  /** The payer for additional rent */
  payer?: Signer;
  /** Authority — must be the asset owner */
  authority?: Signer;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type RegisterX402V1InstructionData = {
  discriminator: number;
  padding: Array<number>;
  url: string;
};

export type RegisterX402V1InstructionDataArgs = { url: string };

export function getRegisterX402V1InstructionDataSerializer(): Serializer<
  RegisterX402V1InstructionDataArgs,
  RegisterX402V1InstructionData
> {
  return mapSerializer<
    RegisterX402V1InstructionDataArgs,
    any,
    RegisterX402V1InstructionData
  >(
    struct<RegisterX402V1InstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 7 })],
        ['url', string()],
      ],
      { description: 'RegisterX402V1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 3, padding: [0, 0, 0, 0, 0, 0, 0] })
  ) as Serializer<
    RegisterX402V1InstructionDataArgs,
    RegisterX402V1InstructionData
  >;
}

// Args.
export type RegisterX402V1InstructionArgs = RegisterX402V1InstructionDataArgs;

// Instruction discriminator.
export const registerX402V1InstructionDiscriminator = 3;

// Instruction.
export function registerX402V1(
  context: Pick<Context, 'eddsa' | 'payer' | 'programs'>,
  input: RegisterX402V1InstructionAccounts & RegisterX402V1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplAgentTools',
    'TLREGni9ZEyGC3vnPZtqUh95xQ8oPqJSvNjvB7FGK8S'
  );

  // Accounts.
  const resolvedAccounts = {
    x402Endpoint: {
      index: 0,
      isWritable: true as boolean,
      value: input.x402Endpoint ?? null,
    },
    agentAsset: {
      index: 1,
      isWritable: false as boolean,
      value: input.agentAsset ?? null,
    },
    payer: {
      index: 2,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 3,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RegisterX402V1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.x402Endpoint.value) {
    resolvedAccounts.x402Endpoint.value = findX402EndpointV1Pda(context, {
      asset: expectPublicKey(resolvedAccounts.agentAsset.value),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRegisterX402V1InstructionDataSerializer().serialize(
    resolvedArgs as RegisterX402V1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
