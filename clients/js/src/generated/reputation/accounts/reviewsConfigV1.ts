/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type ReviewsConfigV1 = Account<ReviewsConfigV1AccountData>;

export type ReviewsConfigV1AccountData = {
  key: Key;
  bump: number;
  padding: Array<number>;
  admin: PublicKey;
  reviewsCollection: PublicKey;
  receiptsCollection: PublicKey;
  nextTreeIndex: bigint;
};

export type ReviewsConfigV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  admin: PublicKey;
  reviewsCollection: PublicKey;
  receiptsCollection: PublicKey;
  nextTreeIndex: number | bigint;
};

export function getReviewsConfigV1AccountDataSerializer(): Serializer<
  ReviewsConfigV1AccountDataArgs,
  ReviewsConfigV1AccountData
> {
  return mapSerializer<
    ReviewsConfigV1AccountDataArgs,
    any,
    ReviewsConfigV1AccountData
  >(
    struct<ReviewsConfigV1AccountData>(
      [
        ['key', getKeySerializer()],
        ['bump', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['admin', publicKeySerializer()],
        ['reviewsCollection', publicKeySerializer()],
        ['receiptsCollection', publicKeySerializer()],
        ['nextTreeIndex', u64()],
      ],
      { description: 'ReviewsConfigV1AccountData' }
    ),
    (value) => ({ ...value, padding: [0, 0, 0, 0, 0, 0] })
  ) as Serializer<ReviewsConfigV1AccountDataArgs, ReviewsConfigV1AccountData>;
}

export function deserializeReviewsConfigV1(
  rawAccount: RpcAccount
): ReviewsConfigV1 {
  return deserializeAccount(
    rawAccount,
    getReviewsConfigV1AccountDataSerializer()
  );
}

export async function fetchReviewsConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<ReviewsConfigV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'ReviewsConfigV1');
  return deserializeReviewsConfigV1(maybeAccount);
}

export async function safeFetchReviewsConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<ReviewsConfigV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeReviewsConfigV1(maybeAccount) : null;
}

export async function fetchAllReviewsConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<ReviewsConfigV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'ReviewsConfigV1');
    return deserializeReviewsConfigV1(maybeAccount);
  });
}

export async function safeFetchAllReviewsConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<ReviewsConfigV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeReviewsConfigV1(maybeAccount as RpcAccount)
    );
}

export function getReviewsConfigV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'mplAgentReputation',
    'REPREG5c1gPHuHukEyANpksLdHFaJCiTrm6zJgNhRZR'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      padding: Array<number>;
      admin: PublicKey;
      reviewsCollection: PublicKey;
      receiptsCollection: PublicKey;
      nextTreeIndex: number | bigint;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      padding: [2, array(u8(), { size: 6 })],
      admin: [8, publicKeySerializer()],
      reviewsCollection: [40, publicKeySerializer()],
      receiptsCollection: [72, publicKeySerializer()],
      nextTreeIndex: [104, u64()],
    })
    .deserializeUsing<ReviewsConfigV1>((account) =>
      deserializeReviewsConfigV1(account)
    );
}

export function getReviewsConfigV1Size(): number {
  return 112;
}

export function findReviewsConfigV1Pda(
  context: Pick<Context, 'eddsa' | 'programs'>
): Pda {
  const programId = context.programs.getPublicKey(
    'mplAgentReputation',
    'REPREG5c1gPHuHukEyANpksLdHFaJCiTrm6zJgNhRZR'
  );
  return context.eddsa.findPda(programId, [
    string({ size: 'variable' }).serialize('program_config'),
  ]);
}

export async function fetchReviewsConfigV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  options?: RpcGetAccountOptions
): Promise<ReviewsConfigV1> {
  return fetchReviewsConfigV1(
    context,
    findReviewsConfigV1Pda(context),
    options
  );
}

export async function safeFetchReviewsConfigV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  options?: RpcGetAccountOptions
): Promise<ReviewsConfigV1 | null> {
  return safeFetchReviewsConfigV1(
    context,
    findReviewsConfigV1Pda(context),
    options
  );
}
