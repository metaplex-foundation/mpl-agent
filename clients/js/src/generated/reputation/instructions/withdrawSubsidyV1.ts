/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findReviewSubsidyPoolV1Pda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type WithdrawSubsidyV1InstructionAccounts = {
  /** Must match the pool's withdraw_authority */
  withdrawAuthority: Signer;
  /** The Core asset representing the agent */
  agentAsset: PublicKey | Pda;
  /** ReviewSubsidyPoolV1 PDA */
  subsidyPool?: PublicKey | Pda;
  /** Where to send the withdrawn lamports */
  destination: PublicKey | Pda;
};

// Data.
export type WithdrawSubsidyV1InstructionData = {
  discriminator: number;
  amount: bigint;
};

export type WithdrawSubsidyV1InstructionDataArgs = { amount: number | bigint };

export function getWithdrawSubsidyV1InstructionDataSerializer(): Serializer<
  WithdrawSubsidyV1InstructionDataArgs,
  WithdrawSubsidyV1InstructionData
> {
  return mapSerializer<
    WithdrawSubsidyV1InstructionDataArgs,
    any,
    WithdrawSubsidyV1InstructionData
  >(
    struct<WithdrawSubsidyV1InstructionData>(
      [
        ['discriminator', u8()],
        ['amount', u64()],
      ],
      { description: 'WithdrawSubsidyV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 3 })
  ) as Serializer<
    WithdrawSubsidyV1InstructionDataArgs,
    WithdrawSubsidyV1InstructionData
  >;
}

// Args.
export type WithdrawSubsidyV1InstructionArgs =
  WithdrawSubsidyV1InstructionDataArgs;

// Instruction discriminator.
export const withdrawSubsidyV1InstructionDiscriminator = 3;

// Instruction.
export function withdrawSubsidyV1(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: WithdrawSubsidyV1InstructionAccounts & WithdrawSubsidyV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplAgentReputation',
    'REPREG5c1gPHuHukEyANpksLdHFaJCiTrm6zJgNhRZR'
  );

  // Accounts.
  const resolvedAccounts = {
    withdrawAuthority: {
      index: 0,
      isWritable: true as boolean,
      value: input.withdrawAuthority ?? null,
    },
    agentAsset: {
      index: 1,
      isWritable: false as boolean,
      value: input.agentAsset ?? null,
    },
    subsidyPool: {
      index: 2,
      isWritable: true as boolean,
      value: input.subsidyPool ?? null,
    },
    destination: {
      index: 3,
      isWritable: true as boolean,
      value: input.destination ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: WithdrawSubsidyV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.subsidyPool.value) {
    resolvedAccounts.subsidyPool.value = findReviewSubsidyPoolV1Pda(context, {
      agentAsset: expectPublicKey(resolvedAccounts.agentAsset.value),
    });
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getWithdrawSubsidyV1InstructionDataSerializer().serialize(
    resolvedArgs as WithdrawSubsidyV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
