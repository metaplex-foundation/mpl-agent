/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findReviewSubsidyPoolV1Pda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DepositSubsidyV1InstructionAccounts = {
  /** Funds the deposit (and pool rent on first init) */
  payer?: Signer;
  /** The Core asset representing the agent */
  agentAsset: PublicKey | Pda;
  /** ReviewSubsidyPoolV1 PDA */
  subsidyPool?: PublicKey | Pda;
  /** Captured as the pool's withdraw authority on first init (defaults to payer) */
  withdrawAuthority?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type DepositSubsidyV1InstructionData = {
  discriminator: number;
  amount: bigint;
};

export type DepositSubsidyV1InstructionDataArgs = { amount: number | bigint };

export function getDepositSubsidyV1InstructionDataSerializer(): Serializer<
  DepositSubsidyV1InstructionDataArgs,
  DepositSubsidyV1InstructionData
> {
  return mapSerializer<
    DepositSubsidyV1InstructionDataArgs,
    any,
    DepositSubsidyV1InstructionData
  >(
    struct<DepositSubsidyV1InstructionData>(
      [
        ['discriminator', u8()],
        ['amount', u64()],
      ],
      { description: 'DepositSubsidyV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 2 })
  ) as Serializer<
    DepositSubsidyV1InstructionDataArgs,
    DepositSubsidyV1InstructionData
  >;
}

// Args.
export type DepositSubsidyV1InstructionArgs =
  DepositSubsidyV1InstructionDataArgs;

// Instruction discriminator.
export const depositSubsidyV1InstructionDiscriminator = 2;

// Instruction.
export function depositSubsidyV1(
  context: Pick<Context, 'eddsa' | 'payer' | 'programs'>,
  input: DepositSubsidyV1InstructionAccounts & DepositSubsidyV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplAgentReputation',
    'REPREG5c1gPHuHukEyANpksLdHFaJCiTrm6zJgNhRZR'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    agentAsset: {
      index: 1,
      isWritable: false as boolean,
      value: input.agentAsset ?? null,
    },
    subsidyPool: {
      index: 2,
      isWritable: true as boolean,
      value: input.subsidyPool ?? null,
    },
    withdrawAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.withdrawAuthority ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: DepositSubsidyV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.subsidyPool.value) {
    resolvedAccounts.subsidyPool.value = findReviewSubsidyPoolV1Pda(context, {
      agentAsset: expectPublicKey(resolvedAccounts.agentAsset.value),
    });
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDepositSubsidyV1InstructionDataSerializer().serialize(
    resolvedArgs as DepositSubsidyV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
