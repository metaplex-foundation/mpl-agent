/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type AgentValidationV1 = Account<AgentValidationV1AccountData>;

export type AgentValidationV1AccountData = {
  key: Key;
  bump: number;
  padding: Array<number>;
  asset: PublicKey;
};

export type AgentValidationV1AccountDataArgs = {
  key: KeyArgs;
  bump: number;
  asset: PublicKey;
};

export function getAgentValidationV1AccountDataSerializer(): Serializer<
  AgentValidationV1AccountDataArgs,
  AgentValidationV1AccountData
> {
  return mapSerializer<
    AgentValidationV1AccountDataArgs,
    any,
    AgentValidationV1AccountData
  >(
    struct<AgentValidationV1AccountData>(
      [
        ['key', getKeySerializer()],
        ['bump', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['asset', publicKeySerializer()],
      ],
      { description: 'AgentValidationV1AccountData' }
    ),
    (value) => ({ ...value, padding: [0, 0, 0, 0, 0, 0] })
  ) as Serializer<
    AgentValidationV1AccountDataArgs,
    AgentValidationV1AccountData
  >;
}

export function deserializeAgentValidationV1(
  rawAccount: RpcAccount
): AgentValidationV1 {
  return deserializeAccount(
    rawAccount,
    getAgentValidationV1AccountDataSerializer()
  );
}

export async function fetchAgentValidationV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AgentValidationV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'AgentValidationV1');
  return deserializeAgentValidationV1(maybeAccount);
}

export async function safeFetchAgentValidationV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<AgentValidationV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeAgentValidationV1(maybeAccount)
    : null;
}

export async function fetchAllAgentValidationV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AgentValidationV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'AgentValidationV1');
    return deserializeAgentValidationV1(maybeAccount);
  });
}

export async function safeFetchAllAgentValidationV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<AgentValidationV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeAgentValidationV1(maybeAccount as RpcAccount)
    );
}

export function getAgentValidationV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'mplAgentValidation',
    'VALREGY66A9ieJfFUNs5GrxFTy498KUoSU7TbmSePQi'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      padding: Array<number>;
      asset: PublicKey;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      padding: [2, array(u8(), { size: 6 })],
      asset: [8, publicKeySerializer()],
    })
    .deserializeUsing<AgentValidationV1>((account) =>
      deserializeAgentValidationV1(account)
    );
}

export function getAgentValidationV1Size(): number {
  return 40;
}

export function findAgentValidationV1Pda(
  context: Pick<Context, 'eddsa' | 'programs'>,
  seeds: {
    /** The address of the asset */
    asset: PublicKey;
  }
): Pda {
  const programId = context.programs.getPublicKey(
    'mplAgentValidation',
    'VALREGY66A9ieJfFUNs5GrxFTy498KUoSU7TbmSePQi'
  );
  return context.eddsa.findPda(programId, [
    string({ size: 'variable' }).serialize('agent_validation'),
    publicKeySerializer().serialize(seeds.asset),
  ]);
}

export async function fetchAgentValidationV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  seeds: Parameters<typeof findAgentValidationV1Pda>[1],
  options?: RpcGetAccountOptions
): Promise<AgentValidationV1> {
  return fetchAgentValidationV1(
    context,
    findAgentValidationV1Pda(context, seeds),
    options
  );
}

export async function safeFetchAgentValidationV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  seeds: Parameters<typeof findAgentValidationV1Pda>[1],
  options?: RpcGetAccountOptions
): Promise<AgentValidationV1 | null> {
  return safeFetchAgentValidationV1(
    context,
    findAgentValidationV1Pda(context, seeds),
    options
  );
}
