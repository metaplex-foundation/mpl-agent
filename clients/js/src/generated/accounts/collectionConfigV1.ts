/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { Key, KeyArgs, getKeySerializer } from '../types';

export type CollectionConfigV1 = Account<CollectionConfigV1AccountData>;

export type CollectionConfigV1AccountData = {
  key: Key;
  bump: number;
  padding: Array<number>;
  collection: PublicKey;
};

export type CollectionConfigV1AccountDataArgs = {
  bump: number;
  collection: PublicKey;
};

export function getCollectionConfigV1AccountDataSerializer(): Serializer<
  CollectionConfigV1AccountDataArgs,
  CollectionConfigV1AccountData
> {
  return mapSerializer<
    CollectionConfigV1AccountDataArgs,
    any,
    CollectionConfigV1AccountData
  >(
    struct<CollectionConfigV1AccountData>(
      [
        ['key', getKeySerializer()],
        ['bump', u8()],
        ['padding', array(u8(), { size: 6 })],
        ['collection', publicKeySerializer()],
      ],
      { description: 'CollectionConfigV1AccountData' }
    ),
    (value) => ({
      ...value,
      key: Key.CollectionConfigV1,
      padding: [0, 0, 0, 0, 0, 0],
    })
  ) as Serializer<
    CollectionConfigV1AccountDataArgs,
    CollectionConfigV1AccountData
  >;
}

export function deserializeCollectionConfigV1(
  rawAccount: RpcAccount
): CollectionConfigV1 {
  return deserializeAccount(
    rawAccount,
    getCollectionConfigV1AccountDataSerializer()
  );
}

export async function fetchCollectionConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<CollectionConfigV1> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'CollectionConfigV1');
  return deserializeCollectionConfigV1(maybeAccount);
}

export async function safeFetchCollectionConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<CollectionConfigV1 | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists
    ? deserializeCollectionConfigV1(maybeAccount)
    : null;
}

export async function fetchAllCollectionConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<CollectionConfigV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'CollectionConfigV1');
    return deserializeCollectionConfigV1(maybeAccount);
  });
}

export async function safeFetchAllCollectionConfigV1(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<CollectionConfigV1[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeCollectionConfigV1(maybeAccount as RpcAccount)
    );
}

export function getCollectionConfigV1GpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'mpl8004Identity',
    '8oo41DdXLnERYxrjU26Byuh3kii6YQY6eqVZUae1Tndk'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      key: KeyArgs;
      bump: number;
      padding: Array<number>;
      collection: PublicKey;
    }>({
      key: [0, getKeySerializer()],
      bump: [1, u8()],
      padding: [2, array(u8(), { size: 6 })],
      collection: [8, publicKeySerializer()],
    })
    .deserializeUsing<CollectionConfigV1>((account) =>
      deserializeCollectionConfigV1(account)
    )
    .whereField('key', Key.CollectionConfigV1);
}

export function getCollectionConfigV1Size(): number {
  return 40;
}

export function findCollectionConfigV1Pda(
  context: Pick<Context, 'eddsa' | 'programs'>,
  seeds: {
    /** The address of the collection */
    collection: PublicKey;
  }
): Pda {
  const programId = context.programs.getPublicKey(
    'mpl8004Identity',
    '8oo41DdXLnERYxrjU26Byuh3kii6YQY6eqVZUae1Tndk'
  );
  return context.eddsa.findPda(programId, [
    string({ size: 'variable' }).serialize('collection_config'),
    publicKeySerializer().serialize(seeds.collection),
  ]);
}

export async function fetchCollectionConfigV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  seeds: Parameters<typeof findCollectionConfigV1Pda>[1],
  options?: RpcGetAccountOptions
): Promise<CollectionConfigV1> {
  return fetchCollectionConfigV1(
    context,
    findCollectionConfigV1Pda(context, seeds),
    options
  );
}

export async function safeFetchCollectionConfigV1FromSeeds(
  context: Pick<Context, 'eddsa' | 'programs' | 'rpc'>,
  seeds: Parameters<typeof findCollectionConfigV1Pda>[1],
  options?: RpcGetAccountOptions
): Promise<CollectionConfigV1 | null> {
  return safeFetchCollectionConfigV1(
    context,
    findCollectionConfigV1Pda(context, seeds),
    options
  );
}
